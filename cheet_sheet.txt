# cmake work

cmake -S orthello -B build/orthello
cmake --build build/orthello
cmake --install build/orthello --prefix ./install/orthello

cmake -S standalone/ -B build/standalone
cmake --build build/standalone
cmake --install build/standalone/ --prefix ./install/standalone

#
# TEST

cmake -S test/ -B build/test
cmake --build build/test
ctest --test-dir ./build/test --output-on-failure

# ls unittest
ctest -N --test-dir ./build/test/

# run unittest with regex, like this will run `*Database*`
ctest -R Database
# run unittest with id
ctest -I 1,2

#
# DOCUMENTATION

cmake -S documentation/ -B build/documentation
cmake --build build/documentation --target GenerateDocs
open build/documentation/doxygen/html/index.html

# gen ctags

ctags -R --langmap=c++:+.cu .

# cpm init

wget -O cmake/CPM.cmake https://github.com/cpm-cmake/CPM.cmake/releases/latest/download/get_cpm.cmake




set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DDEBUG")
